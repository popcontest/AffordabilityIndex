# Database
DATABASE_URL="postgresql://user:password@localhost:5432/affordability_index"

# Census API (optional - get free key at https://api.census.gov/data/key_signup.html)
CENSUS_API_KEY=""

# Site URL (for canonical URLs and metadata)
NEXT_PUBLIC_SITE_URL="https://affordabilityindex.org"

# Security Configuration

# Rate Limiting (Upstash Redis) - Required for production rate limiting
# Get your credentials at: https://upstash.com/
# If not configured, will use in-memory rate limiting (NOT production-ready)
UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""

# Environment
NODE_ENV="production"

# Error Tracking (Optional) - Sentry integration
# Sign up at: https://sentry.io/
SENTRY_DSN=""
SENTRY_AUTH_TOKEN=""
SENTRY_PROJECT=""
SENTRY_ORG=""
